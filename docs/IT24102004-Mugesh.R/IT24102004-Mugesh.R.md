# IT24102004-Mugesh.R Setup and Validation Record

- **Name / Student ID:** Mugesh Ravichandran — IT24102004
- **Branch:** IT24102004-Mugesh.R
- **Date:** September 22, 2026

## Summary of Changes
1. **Dockerfile:**
   - Base image set to `php:8.4-apache-bookworm` with Composer 2.
   - Updated package mirrors to use HTTPS to avoid Fastly CDN network blocks.
2. **Compose Configuration (`compose.yml`):**
   - Configured `dvwa` service to build locally (`image: dvwa-team:local`, `pull_policy: build`).
   - Replaced hardcoded credentials with runtime environment variables (`DB_PASSWORD`, `MYSQL_ROOT_PASSWORD`).
3. **Docker Secret Exclusions (`.dockerignore`):**
   - Excluded `.env`, runtime configurations (`config/config.inc.php`), and artifacts (`evidence/`, `reports/`) from the Docker image build context.

## Build Validation
- **Build Status:** Succeeded (Exit code: 0)
- **Evidence Files:**
  - `evidence/setup/docker-version.txt`
  - `evidence/setup/compose-version.txt`
  - `evidence/setup/IT24102004-Mugesh.R-build.txt`
  - `evidence/setup/image-id.txt`

## AI Usage Disclosure
- AI assistance was utilized to troubleshoot Debian mirror network blocks and draft documentation.
- All configurations, container builds, and git workflows were manually inspected, executed, and verified locally.
